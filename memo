0930

요즘 자바 스타일
짦게 짧게 만듬
옛날 자바는 참조만 할것

중국 속담 따뜻한 차를 마시고 싶으면 기존의 차를 비워라

객체
메모리를 차지하고 있으면 객체
좁은 의미: 클래스 방식
객체(인스턴스)

js
객체리터럴 / 포로토타입 / 클래스
객체리터럴 : 키 값으로 구성됨 ->묶는다

why ->의미를 모아 단위
what
how

구조 - 구조체 - 클래스(vo, dto, entity)

머신러닝
KNN( nearest neighbor)

영화별 키스씬, 액션씬 표 관련하여

자바로 만든다면 무조건 클래스로 만들어야 함

자바
상속 기법
불고기 피자 extends 피자

js
객체-속성(property, attribute)

html
<h1 class = "ddd" id = "aaa">Index Page</h1>
객체임

퀵정렬 외워야 함
정렬 알고리즘 비교

1030
함수
시작점 패러다임
절차지향적 패러다임
함수형 패러다임
함수형 언어
코어를 봄 / 덩어리 로직을 봄

함수
원의 넓이(이름) (입력) (출력)

자바에 함수는 없다?

void: 사실 기본 자료형

함수: statement expression

함수와 변수가 다르지 않다

return의 의미는 : 실행의 제어권의 반납

자바파일 역컴파일 방법
역컴파일 파일에 load, goto,

메인 함수를 만들기 위해 껍데로 클래스를 만들어야 함

요즘 자바 클래스 스타일
Main 클래스를 하나 설계하여 Main class를 찾아서 main 함수를 찾아서 실행 함

fs 함수
function 이 붙음

함수의 이름 = 변수 이름

함수가 변수면 배열로 만들 수 있다?

1130

함수 스택

int add(int a, int b){
return a+ b
}
int x = 10;
int y = 100;
int result = add(x, y);

->이건 단순희 int 메모리 공간 3개
->result에 반환값 만 남기고, 다른 지역변수는 메모리 공간에서 사라님

자바 창시자 고슬링에서 메일 보냄-> 답장 옴

지역변수: 함수안에 선언 되어 있는 변수

함수는 로직을 위해 코드를 묶어놓은 덩어리 ㅇ;ㅣ다
변수로 할당 =>함수 이름
함수는 () ->실행 ->메모리 공간 ->필요한 변수 =>

사실 변수 = 합수
함수 ->1급 객체(first class object)

화살표 함수

js 함수 박성룔

1430
클래스 파일
클래스 로더
스콜라, lips

static 정적인, 이 키워드 있으면 클래스에 붙음

Main 클래스(main 함수, add 함수)

메모리 스택

스택 오버플로우: 메모리가 가득 차는 현상

함수 ->로직 단위 ->(문/식) -> {} ->이름
function

브래이스 {}

function makeSum (arr) {

}

score = [10, 20, 30, 40]
makeSum(score)


1. 정해진 숫자만큼 출력하는 함수 만들기 printNums(start, end)



2. 로또 번호 생성 함수 만들기

3. 배열을 주면 max값을 찾아내는 함수 만들기
getMaxInArray(arr){

return max;

}


4. 배열을 주면 min값을 찾아내는 함수 만들기

5. 매출/월

1530
함수를 파라미터로 던질수 있다.

calc(10, 20, (x, y) => x + y )
calc(10, 20, (x, y) => x - y )
의미 이해하기

자바스크립트 면접 문제
1. 호이스팅

2. 클로져 : 함수를 이용해서 돼지 저금통을 만드는 것

감춰진 참조값

원래 지역변수라 없어져야 하지만 매개별수가 계속 사용중이므로
마치 자바 처럼 상태를 유지하고 있음