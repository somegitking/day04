1. 줄바꿈(개행) 출력
    console.log(i) i => 숫자, 문자, 문자열
    console.log(`${i}`) i => 숫자, 문자, 문자열

2. 줄바꿈 없이 출력
    process.stdout.write(i) i => 숫자 안됨, 문자 가능, "" 가능, ''가능
    process.stdout.write(`${i}`) i => 숫자 가능

3. 문자열 입력 받기
const read = require('readline-sync') ->import와 비슷한 역할
let num = read.question(`Enter No.: `) ->문자열 입력 받아 num에 저장
** 숫자 입력하면 숫자 저장
** 문자 입력하면 문자 저장